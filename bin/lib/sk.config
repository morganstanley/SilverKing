                      skJavaHome=
                        skExecv2=
        skAdminJavaCommandHeader="$skExecv2"
export skDaemonJavaCommandHeader="$skExecv2"
# only necessary for skfs (starting/stopping), sk doesn't need this set. and we need it here instead of skfs.config b/c of dist. we don't set any env in dist externally, so we need to set internally.
export              SK_JACE_HOME=
                      STATS_FILE=